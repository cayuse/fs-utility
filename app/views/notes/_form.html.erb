<%= form_for(@note, :remote => true) do |f| %>
  <div id= "note_errors" style="display:none"></div>

  <p>
    <%= f.label :entry %><br />
    <%= f.text_area :entry, :rows => 5 %>
  </p>
  <p><%= f.submit %></p>
<% end %>