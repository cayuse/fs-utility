<h1>New allergen</h1>

<%= form_for(@allergen) do |f| %>
  

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', allergens_path %>