<h1>Editing allergen</h1>

<%= form_for(@allergen) do |f| %>
  

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @allergen %> |
<%= link_to 'Back', allergens_path %>